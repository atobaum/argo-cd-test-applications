apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
  annotations:
    reloader.stakater.com/auto: "true"
    argocd.argoproj.io/sync-wave: "1"   # 나중: Secret 준비된 뒤 배포
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      serviceAccountName: demo-app
      containers:
        - name: echo
          image: mendhak/http-https-echo:latest
          env:
            - name: HTTP_PORT
              value: "8080"
            - name: ECHO_INCLUDE_ENV_VARS
              value: "true"
          envFrom:
            - secretRef:
                name: demo-secret   # secret 전체 키를 env로 마운트 (GREETING 등)
          ports:
            - containerPort: 8080
